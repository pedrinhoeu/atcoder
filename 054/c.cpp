#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    cin.tie(0)->sync_with_stdio(0);
    string pala;
    cin >> pala;
    cout << (pala[0] - '0') + (pala[1] - '0') + (pala[2] - '0') << "\n";
}